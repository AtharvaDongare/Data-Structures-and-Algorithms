// Impelement a hash map consisting of all the elements from the list1
// once we have the hashmap then we can iterate over the second list
// while we are iterating over the second list we will keep looking into the hashmap
// the first occurance of node which is there on both sides will be returned since that is the value