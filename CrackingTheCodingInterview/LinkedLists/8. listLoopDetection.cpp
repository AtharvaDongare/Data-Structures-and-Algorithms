// use the tortoise and hare approach
// one fast pointer which skips over one node and the second slow pointer traveling at normal speed
// if they meet then the list has a loop within itself, if not then they dont have a loop
